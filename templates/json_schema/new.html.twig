{% extends 'layout.html.twig' %}

{% set pageTitle = 'json_schema.new' %}
{% set pageSubTitle = 'Gestion des schémas JSON' %}

{% block wrapper %}
    <div class="row">
        <div class="col-md-4">
            <!-- START card-->
            <div class="card card-default">
                <div class="card-header">{{ 'Paramétrage'|trans }}</div>
                <div class="card-body">
                    {{ include('json_schema/_form.html.twig', {'button_label': 'action.save'|trans}) }}
                    <a class="btn btn-primary" href="{{ path('json_schema_index') }}">{{ 'action.back_to_list'|trans }}</a>
                </div>
            </div><!-- END card-->
        </div>
    </div>
{% endblock %}
